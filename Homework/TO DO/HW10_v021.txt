Добрый день, Алексей !  
Спасибо за выполненную работу, реализация верная, но давайте проведем небольшой рефакторинг кода. 


+++ 1) Разгрузить - case и для всех команд создать свои методы в которые перенести текущую логику. 
        По аналогии с методом - ListTasks() для команды - /showtasks. 
+++ 2) Метод ListTasks() сделать с возвращающим значением - bool, учесть это при реализации метода - для команды /removetask. 
+++ 3) Для команд - /start, /addtask добавить проверку - string.IsNullOrWhiteSpace() 
+++ 4) Для команды - /echo, заменить проверку - cmd.IndexOf("/echo") >= 0 на - cmd.StartsWith("/echo") 
+++ 5) Для команды - /info строку - "Версия программы: 0.2, дата создания: 09.09.2025.", перенести в глобальную константу. 
+++ 6) После того как создадите все методы для команд у вас должны переменные - todoList, name стать глобальными.

Cоветую еще в методе - AddTask(), 
добавление в список - todoList.Add(taskText); вынести за пределы цикла - while (string.IsNullOrWhiteSpace(taskText)), 
так код будет более эффективным.